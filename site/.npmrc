# Cast UI ships react-native as a peer dependency. On the web we satisfy it with
# react-native-web via a bundler alias (see vite.config.ts), so we never install
# the real react-native package. legacy-peer-deps keeps npm from trying to
# auto-install it and from erroring on the (intentionally) unmet peer.
legacy-peer-deps=true
